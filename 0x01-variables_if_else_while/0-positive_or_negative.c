#include <stdlib.h>
#include <time.h>
/**
 * main -this is the main function
 * return : always zero

*/
/*more headers goes there*/
int main (void)
{       
        int n;
        srand (time(0))
        n = rand() - RAND _MAX / 2;
        if (n > 0)
        {       
                printf("%d is positve\n", n);
        }       
        else if (n == 0)
        {       
                printf("%d is zero\n", n);
        }       
        else

}

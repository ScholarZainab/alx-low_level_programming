#include <stdlib.h>
#include <time.h>
#include <studio.h>
/**
 * main-Determines if a number is positive , negative or zero .
 *
 * Return: Always 0 (Sucess)
 */
int main (void)
{       
        int n;
        srand (time(0))
        n = rand() - RAND _MAX / 2;
        if (n > 0)
        {       
                printf("%d is positve\n", n);
        }       
        else if (n == 0)
        {       
                printf("%d is zero\n", n);
        }       
        else

}
